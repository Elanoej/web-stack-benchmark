spring:
  application:
    name: spring-webflux-kotlin

  r2dbc:
    url: ${SPRING_R2DBC_URL:r2dbc:postgresql://localhost:5432/benchmark}
    username: ${SPRING_R2DBC_USERNAME:bench}
    password: ${SPRING_R2DBC_PASSWORD:bench123}
    pool:
      initial-size: 5
      max-size: 20
      max-idle-time: 10m

server:
  port: 8080